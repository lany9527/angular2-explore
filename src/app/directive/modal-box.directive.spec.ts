import { ModalBoxDirective } from './modal-box.directive';

describe('ModalBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalBoxDirective();
    expect(directive).toBeTruthy();
  });
});
